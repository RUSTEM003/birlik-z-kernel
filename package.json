{
  "name": "birlik-z-kernel",
  "version": "1.0.0",
  "description": "Comprehensive global digital banking platform with Z-KERNEL architecture",
  "private": true,
  "workspaces": [
    "apps/*",
    "core/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "eslint": "^8.40.0",
    "typescript": "^5.0.4",
    "madge": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
